# TODO

## 今やる

- [ ] Home の大ボタンを `文法授業 / 英単語 / 英文添削` の見せ方へ寄せる
- [ ] Study / Dialogue 側の実授業フローに `エマ確認クイズ` を差し込む
- [ ] 正解時の `好感度上昇表示` をどこで見せるか決める
- [ ] Home のおすすめ表示を `review + エマ優先` に調整する
- [ ] エマ専用ボイス仮置きをどこから先に差し替えるか決める
- [ ] 一時的な `emma -> noah` 音声フォールバックを外せるよう、既存音声を `emma` 名前空間へ移す
- [ ] `見た目カスタムMVP` を `エマ固定 + 衣装 / 背景 / アクセ` 方針で実装に落とす
- [ ] エマ用の最初の `衣装 / 背景 / アクセ` 解放セットを決める
- [ ] 好感度報酬とガチャ報酬で `衣装 / 背景 / アクセ / ボイス / 思い出` をどう配分するか決める
- [ ] Tutorial のエマ立ち絵の収まりを端末別に確認する
- [ ] Tutorial の下端固定レイアウトが低い画面高でも崩れないか確認する
- [ ] Tutorial の導入会話を本編ストーリーとどう接続するか決める
- [ ] エマの `表情6個 + 待機2個 + 反応2個` をどの画面から先に使うか決める
- [ ] `NoaChatBox` をキャラ別に分離するか、エマ専用の会話UIを別で持つか決める
- [ ] `NoaChatBox` / `noaChatLimits` / `createNoaChatResponse` など Noah 名の残骸をどこまで改名するか決める

## 次にやる

- [ ] `Heroine Select` をMVPから外すか、将来拡張用に残すか決める
- [ ] Home / 学習リザルト / チュートリアル のUI優先順位をそろえる
- [ ] エマ立ち絵 / 背景 / 小物の参考アセットを `ART_STYLE.md` 基準でそろえる
- [ ] 英単語クイズを `エマ伴走体験` に寄せる
- [ ] 英文添削にエマコメント導線を付ける
- [ ] 好感度しきい値で個別会話を解放する
- [ ] ガチャ報酬を `ストーリー / アルバム / ボイス解放` の本導線へつなぐ

## 保留

- [ ] `信頼` など好感度以外の関係値を追加するか決める
- [ ] 会話練習モードを初期MVPに入れるか決める
- [ ] Live2D を初期MVPに入れるか、まず静的立ち絵で進めるか決める
- [ ] 複数ヒロイン案を将来拡張としてどこまで残すか決める
- [ ] ノート命名ルールを決める

## 完了

- [x] Obsidian を docs Vault として設定
- [x] 正規作業場所を `C:\dev\study-musume` に固定し、退避コピーから必要差分を救出して `main` へ戻した
- [x] `実装AI -> レビューAI -> 修正 -> Codexで再確認とcommit` の terminal-first 運用を `AGENTS.md` に明文化した
- [x] AI が作業前に読む docs の順番を `AGENTS.md` に明文化した
- [x] 開発メモと仕様整理の土台を作成
- [x] 英語学習 x 複数ヒロインの中核仕様を整理
- [x] Home ベースのワイヤー方針を作成
- [x] `selectedHeroineId` を `gameStats` のトップレベルに置く方針を決めた
- [x] Home に `指定ヒロイン枠` を入れた
- [x] ヒロインを `分野担当制` ではなく `推しで全学習モードを遊べる` 方針に決めた
- [x] 各ヒロインの `共通序盤イベント 3本 + ルート導入 1本` の骨格を決めた
- [x] 各ヒロインの `立ち絵要件 / 表情差分 / 画面での役割` を整理した
- [x] 初回チュートリアルを `推し選択 -> 学習 -> クイズ -> 好感度上昇 -> 次の約束` の流れで固めた
- [x] `高瀬エマ` 単独ヒロイン案の Home 文言と初回チュートリアル台本を作った
- [x] MVP を `高瀬エマ単独ヒロイン案` で固定した
- [x] `Aimy 風のフルキャラデザ` は見送り、`エマ固定 + 衣装 / 背景 / アクセ` の軽量カスタムMVP方針を決めた
- [x] `キャラ / 背景 / 小物` を同じ空気で見せるための全体アート方針を docs に追加した
- [x] エマの MVP 表情差分と最小モーション方針を docs に追加した
- [x] `エマ単独MVP` に寄せる5ファイル限定の実装メモを作った
- [x] `Tutorial.jsx / tutorialData.js / TutorialHome.jsx / Home.jsx / gameLoopUtils.js` をエマ単独MVP向けに最小実装した
- [x] Tutorial を `教室背景 + エマ立ち絵 + 下部会話パネル` のソシャゲ風UIへ作り直した
- [x] Tutorial を `スクロールしない固定画面 + 魔女Live2D表示` に寄せた
- [x] Tutorial のメタUIを減らし、導入会話を少し厚くした
- [x] Tutorial を `下端固定の会話 / 問題UI + 上側をキャラ領域に使う` レイアウトへ組み替えた
- [x] SoundContext の volume 補間をクランプして `IndexSizeError` を防ぐようにした
- [x] ガチャ画面を `学習のごほうび導線` 前提のUIに整理した
- [x] Home / Tutorial のエマ表示を `静止画 + 疑似待機モーション` ベースへ寄せた
- [x] エマの `blink` 差分を追加して Home / Tutorial の自動まばたきへつないだ
- [x] `selectedHeroineId / favoriteCharacter` の保存不整合を起きにくくした
- [x] `emma-mvp` の Home で Noah 専用導線へ誤って入る箇所を仮止めした
- [x] スマホ版ガチャに `履歴 / 提供割合` の導線を戻した
- [x] `emma` を Tutorial 保存値と Home / Opening / 好感度反応 / タップ反応 / 関係イベントの最小データ層まで通した
- [x] エマ音声は `emma/...` 名義で参照しつつ、再生時だけ `noah/...` へ一時フォールバックする移行ラインを入れた
- [x] Home 下のチャットを、同じUIのまま `characterId` ベースでエマにも戻した
- [x] `03_CHARACTERS` の docs を `高瀬エマ` 単独前提に整理した
