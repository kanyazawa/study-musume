# Project Status

## 現在の主作業

- 作品全体を `放課後 x 英語学習 x 恋愛寄り伴走体験` として、`高瀬エマがずっと隣で見てくれる学習恋愛ゲーム` に寄せている
- Home 画面を `エマ立ち絵 + 学習開始3導線 + review優先 + 次イベント予告` の形に寄せつつ、単独ヒロイン体験を先に成立させる段階に入った
- 当面の主戦場は `キャラ立ち絵`, `キャラ別ストーリー`, `チュートリアル`, `各種UIの詰め` の4本

## いま実装で触っている場所

- `src/data/tutorialData.js`
- `src/pages/Tutorial.jsx`
- `src/pages/TutorialHome.jsx`
- `src/pages/Home.jsx`
- `src/pages/Gacha.jsx`
- `src/pages/Gacha.css`
- `src/utils/gameLoopUtils.js`
- `src/utils/tutorialStorage.js`

## 現在の到達点

- Home に `日付 / 時間帯 / 注目キャラ / 約束` を見せる方向性は入っている
- Home に `selectedHeroineId` を使った `指定ヒロイン枠` を表示し、変更導線も置いた
- `review優先` と `約束優先` の分岐ロジックは `gameLoopUtils` 側で扱い始めている
- `selectedHeroineId` は `gameStats` のトップレベルで持ち、旧 `favoriteCharacter / characterId` から復元する方針にした
- 関連テストと build は通る状態
- `高瀬エマ` を単独ヒロインMVPの看板キャラとして採用する方針を決めた
- `Aimy 風のフルキャラデザ` は初期MVPではやらず、`エマ固定 + 衣装 / 背景 / アクセ` の軽量カスタムで進める方針を docs に追加した
- `高瀬エマ` のキャラ設定、Home 文言、初回チュートリアル台本は docs に入っている
- MVP は `推し選択` ではなく `最初からエマがいる` 形に固定した
- `キャラ / 背景 / 小物` を `やわらかく上品なアニメ調` で統一する全体アート方針を docs に追加した
- エマのMVP表情差分は `表情6個 + 待機2個 + 反応2個` を最小セットにする方針を docs に追加した
- 初回チュートリアルの `Home -> 学習 -> クイズ -> 好感度上昇 -> 次の約束` 導線は docs に入っている
- `Tutorial.jsx / tutorialData.js / TutorialHome.jsx / Home.jsx / gameLoopUtils.js` の5ファイルに絞った実装メモを docs に入れた
- チュートリアルは `推し選択 -> 英単語 -> ガチャ` から、`エマ導入 -> 文法確認 -> 好感度上昇 -> 次の約束` へ実装を切り替えた
- Tutorial は `説明カードを並べる画面` から、`教室背景 + エマ立ち絵 + 下部会話パネル` のソシャゲ風レイアウトへ作り直した
- Tutorial の `オープニング / 結果` は1行ずつ送る会話進行に変えて、`最初の学習シーン` としての見た目を強めた
- Tutorial は `縦スクロールしない固定画面` を前提に詰め直し、会話パネルや問題UIも同じ1画面内に収めた
- Tutorial のキャラ表示は `魔女 Live2D + 魔女帽子アクセ` を使う前提へ寄せた
- Tutorial の `DAY 1 / 好感度 / ジェム / 進行度` などメタ情報は引いて、導入会話と空気感を優先する見せ方に寄せた
- Tutorial の導入台詞は `なぜエマが主人公を見るのか` が分かるように少し厚くした
- Tutorial は `上 = キャラ表示 / 下 = 会話と問題` の固定レイアウトへ寄せて、下端の会話ボタンが潰れにくい構造へ組み替えた
- Home は `tutorialHomeVariant: emma-mvp` で、エマ名義の見せ方に最低限寄せた
- Home / Tutorial のキャラ表示は、当面 `Live2D前提` ではなく `エマ静止画 + 疑似待機モーション` で見せる方向に切り替えた
- `StaticCharacterImage` にエマ用の受け口と軽い待機モーションを足し、表情差分が揃うまでの仮運用ラインを作った
- エマの `blink` 差分を1枚追加し、Home / Tutorial の疑似待機モーションで自然にまばたきする最低ラインを入れた
- ガチャ画面は `ワイヤー確認用の仮文言` を外し、`学習のごほうび導線` として報酬タイプが見える構成へ整理した
- ガチャ結果から `アイテム / ボイス確認` へ戻る導線を足し、重複時は `NEW` 表示が乱れないようにした
- SoundContext の BGM フェード補間と SE / Voice 再生時の volume 値をクランプして、`volume outside the range [0, 1]` の実行時エラーを潰した
- `selectedHeroineId / favoriteCharacter` の保存値は再選択時にそろうように寄せ、旧 save の復元時も不整合が残りにくい形へ調整した
- Tutorial 完了時の保存は `characterId / selectedHeroineId / favoriteCharacter = emma` でそろえ、エマを正式な `characterId` として通し始めた
- Home / Opening / 好感度反応 / タップ反応 / 関係イベントにも、エマ用の最小データ層を追加した
- `emma-mvp` の見せ方は `characterId === emma` のときだけ効くようにし、古い variant が残っても別キャラ表示に誤爆しにくくした
- Noah 専用のチャット導線は `characterId === noah` のときだけ出すように寄せた
- エマの Home / タップ / 学習リアクション音声は `emma/...` 名義で参照しつつ、実ファイル未移行中だけ再生時に `noah/...` へ一時フォールバックする形へ寄せた
- Home 下の `NoaChatBox` は見た目をそのまま使いながら、`characterId` に応じて `エマ / ノア` の表示名・履歴キー・会話導入・APIプロンプトを切り替える形へ寄せた
- スマホ版ガチャに `ガチャ履歴 / 提供割合` の導線を戻して、モバイルで隠れていた機能を再び触れるようにした
- `03_CHARACTERS` の docs は `高瀬エマ` 単独前提に整理し、他ヒロイン案のメモは一旦外した
- build は通る状態
- ただし `エマ個別ルートの中盤以降`, `NoaChatBox のエマ版`, `エマ専用ボイス差し替え`, `既存複数ヒロイン前提UIの整理` はまだ薄い

## 次に詰めること

- Home のおすすめ表示を `review + エマ導線優先` に寄せる最終調整
- Study / Dialogue 側の実授業フローにも `エマ確認クイズ` を差し込む
- Tutorial / Home のエマ立ち絵の収まりと疑似待機モーションが端末ごとに崩れないか確認する
- Tutorial の下端固定レイアウトが縦の短い端末でも崩れないか確認する
- Home の大ボタンを `勉強` 1本から `文法 / 英単語 / 英文添削` の見せ方へ寄せる
- ガチャ報酬を `ストーリー / アルバム / ボイス解放` の本実装へつなぐ
- `CharacterInteraction` のカスタマイズを、エマ向けの `見た目ごほうび導線` としてどこまで先に通すか決める
- エマ立ち絵, 背景, 小物を新しい `ART_STYLE.md` 基準でそろえる
- エマ用の最初の `衣装 / 背景 / アクセ` 解放セットを何にするか決める
- Tutorial / Home でエマの `smile / shy / troubled` など次の表情差分をどこまで先に使うか整理する
- `NoaChatBox` を分離して、エマでも自然な会話導線を出せるか決める
- 一時フォールバックを消せるよう、既存 Noah 音声を `emma` 名前空間へ移す
- `NoaChatBox` という名前のまま残っているコンポーネントや util を、どこまで `CompanionChat` 系へ改名するか決める

## UI 上の注意

- Home.jsx の DOM 構造はなるべく維持
- class 名変更は禁止
- `学習アプリ一覧` ではなく `ゲームのホーム画面`
- 大ボタンは既存 Home アセット流用を優先
