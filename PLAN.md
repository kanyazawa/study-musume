# Study Musume 全体 PLAN

## Summary
- アプリ全体は「対戦中心のゲーム化学習アプリ」として再定義する。
- 長期目標は `試験攻略`、日々の主ループは `学習で強くなる -> 対戦/復習で確認する -> 報酬で育成する -> 次の学習へ進む` に統一する。
- 新しい独立ゲームを追加する前提ではなく、既存の `学習 / 復習 / 単語バトル / キャラ育成 / ランキング / ミッション` を一つのゲームループとして整理する。
- `PLAN.md` では、アプリ全体を1本の“ブラウザゲーム”として扱い、各画面を役割ごとに再配置して定義する。

## Core Design
- ゲームの目的:
  - 試験日までに学習力を高め、対戦と復習で定着度を上げながら、最終的に試験攻略を達成する。
- プレイヤー操作:
  - `ホーム` で次の行動を選ぶ。
  - `授業` で新規知識を獲得する。
  - `復習` で弱点を回収する。
  - `単語バトル` で習得度を実戦確認する。
  - `キャラ/ミッション/ショップ` で報酬を育成へ変換する。
- メインループ:
  - 学習選択 -> 学習プレイ -> 正誤/進捗保存 -> 弱点生成 -> 復習または対戦 -> 報酬獲得 -> TP/好感度/所持資源更新 -> 次の目標提示。
- 勝利条件:
  - 長期: 試験日までに想定学力帯へ到達し、主要学習範囲を消化する。
  - 中期: 学習ランク上昇、苦手数減少、対戦成績向上、ミッション達成。
  - 短期: 1セッションごとの正答率目標、連勝、レビュー完走。
- 失敗条件:
  - 1プレイ単位では `不正解 / 時間切れ / セッション未達`。
  - メタ進行では `TP不足で十分に周回できない / 復習が滞留する / 試験日までの進捗不足`。
  - ただし全体としてはゲームオーバー制ではなく、立て直し可能な継続型にする。
- 難易度の上がり方:
  - 学習範囲の拡大。
  - 出題速度・問題数・選択肢の紛らわしさの上昇。
  - 復習対象の蓄積による管理負荷の増加。
  - 対戦相手レートや級の上昇。
- 画面構成:
  - `タイトル`: 世界観と開始導線。
  - `ホーム`: 今日の推奨行動、試験カウントダウン、復習導線、対戦導線。
  - `授業選択/授業`: 知識獲得フェーズ。
  - `復習`: 定着フェーズ。
  - `単語バトル`: 実戦確認フェーズ。
  - `ミッション/ランキング/プロフィール/キャラ`: 中長期モチベーション維持。
- 必要なアセット:
  - 既存キャラ画像/Live2D、背景、UIアイコン、既存SE/BGM/ボイスを優先再利用。
  - 新規アセットは原則 `最小限の状態表示アイコン` と `進捗演出` のみ。
- 技術スタック:
  - 既存の `React 19 / Vite 7 / React Router / localStorage saveUtils / Firebase / CharacterStage / existing vocab/review utilities` を継続利用。
  - 新規基盤は増やさず、既存ページと保存構造の整理を優先する。

## Character Expression Direction
- 作品全体のキャラ表現は `清純ベース + 少しだけドキッとする親密さ` を基本方針にする。
- キャラの魅力は `性的魅力` より `感情的魅力` で作る。
  - 優しさ、努力、照れ、信頼、応援、安心感、特別扱いを主要な訴求軸にする。
- 関係性は `恋愛未満の特別感` を上限にする。
  - `あなたは特別`, `一緒に頑張ろう`, `来てくれてうれしい` のような親密さは出してよい。
  - 露骨な誘惑、性的な誘い方、性的満足を主目的にした演出には寄せない。
- ドキッとさせる演出は `視線`, `間`, `表情`, `言い回し` で作る。
  - 上目づかい、照れ、少し言い直す、名前を呼ぶ、軽い独占欲のような演出は許容する。
  - 胸、尻、太ももなど身体部位の誇張を主軸にしない。
- セリフは `応援と伴走` を中心に設計する。
  - 褒め、励まし、見守り、学習後コメント、時間帯ボイスをキャラ魅力の中心に置く。
  - あざとさは補助的に使い、常時のトーンは崩さない。
- ビジュアルは `顔と表情が主役` になるようにする。
  - 立ち絵、Live2D、イベントCG、サムネイル、広告クリエイティブでも表情差分を訴求の中心にする。
  - 露出、透け、下着連想、過度な身体ライン強調は避ける。
- 衣装は `清潔感`, `個性`, `季節感`, `生活感` を優先する。
  - 制服風、私服、部屋着、季節イベント衣装はよいが、性的連想が強い衣装は主軸にしない。
- 報酬設計は `露出解放` ではなく `親密度解放` を基本にする。
  - 特別ボイス、表情差分、個別ストーリー、呼び方変化、思い出会話を主なご褒美にする。
- マーケティングでは `えっちさ` より `この子との会話が良い`, `反応がかわいい`, `続けると距離が縮まる` を話題化ポイントにする。
- 教育アプリとしての信頼と配信プラットフォーム適合性を優先し、`エロ売り` を作品の中心的なブランド戦略にはしない。

### Character Expression Guardrails
- `勉強を続けたくなる親密さ` は歓迎するが、`性的満足を目的にした表現` にはしない。
- 学習や成長よりも性的ご褒美が主報酬に見える設計は避ける。
- 学生っぽさが強い印象と性的訴求が結びつく演出は避ける。
- 下ネタ、身体いじり、過度な密着、挑発的な文言をギャグや広告で安易に使わない。

### Marketing Tone Guide
- 広告、ストア説明文、SNS投稿、動画サムネイルでは `えっちさ` ではなく `親密さ`, `継続したくなる空気`, `会話の心地よさ` を訴求する。
- 主な訴求軸:
  - `この子が応援してくれるから勉強が続く`
  - `会話がかわいい`
  - `続けるほど距離が縮まる`
  - `勉強の達成感がそのままキャラとの思い出になる`
- コピーは `優しい`, `近い`, `少し照れる`, `でも品がある` トーンに寄せる。
- 強い言い回しを使う場合も、刺激ではなく感情の余韻を残す方向にする。
  - 例: `こんなに褒めてくれる学習アプリ、ずるい`
  - 例: `勉強しに来たはずなのに、この子との会話が楽しみになる`
  - 例: `頑張るほど、少しずつ特別になっていく`
- 避ける訴求:
  - 身体訴求を主役にするコピー
  - 露骨に釣るサムネイルや切り抜き
  - `ご褒美` を性的含意で見せる文言
  - `過激`, `刺激的`, `ムラムラ` のような露骨な連想語

### Dialogue Template Rules
- 台詞の基本役割は `学習継続を気持ちよく支えること` に置く。
- 会話カテゴリは、まず以下の4系統を中心に整備する。
  - `歓迎`: ログイン時、再訪時、時間帯別のひとこと
  - `伴走`: 学習前の背中押し、途中の励まし、終了後のねぎらい
  - `親密`: 好感度上昇に応じた特別感、呼び方変化、思い出言及
  - `軽いあざとさ`: たまに照れる、少し拗ねる、少しだけ独占欲を見せる
- 台詞配分は `安心感 70 / 特別感 20 / あざとさ 10` を目安にする。
- 基本文体:
  - 短めで言い切る
  - 命令より提案を使う
  - 否定より受容を先に置く
  - 一文のどこかに `見ている`, `気づいている`, `一緒にいる` 感を入れる
- よく使う感情タグ:
  - `褒める`
  - `励ます`
  - `安心させる`
  - `少し照れる`
  - `少し拗ねる`
  - `特別扱いする`
- 台詞の基準例:
  - 学習前: `今日も来てくれたんだ。少しずつでも、ちゃんと進んでるよ`
  - 正解後: `いいね。その調子でいこう。今の、すごく良かったよ`
  - 学習後: `おつかれさま。頑張ったの、私がちゃんと覚えてる`
  - 軽い親密: `えへへ、来てくれるとやっぱりうれしい`
  - 軽いあざとさ: `今日はちょっとだけ、私を優先してくれる?`
- 避ける台詞:
  - 露骨な誘惑
  - 身体や性的想像を直接促す言い回し
  - 学習の文脈を壊す強すぎる依存表現
  - 脅しや罪悪感を使って継続を迫る文言

## Key Changes
- 画面役割の再定義:
  - `Home` を単なるハブではなく、日次ゲームループの司令塔にする。
  - `StudySelect + Dialogue/Reading/Writing` を育成フェーズとして統一的に扱う。
  - `Review` を敗北の後始末ではなく、対戦前後の必須強化導線として位置づける。
  - `MultiplayerMatch` を“派生機能”ではなく主戦場の1つとして扱う。
- 進行指標の整理:
  - 主要KPIを `試験進捗 / 学習ランク / 復習負債 / 対戦成績 / 継続日数` に集約する。
  - 画面ごとに増える値ではなく、全体ループに返ってくる値として整理する。
- 予定する公開インターフェース/型の変更方針:
  - `stats` にアプリ全体の主ループを表す集約値を持たせる。
  - 候補: `examProgress`, `battleProgress`, `reviewLoad`, `dailyGoals`, `recommendedNextAction`。
  - 既存保存形式は互換維持し、未定義時はデフォルト補完する。
- `PLAN.md` に明記する実装原則:
  - 新規大型機能追加より、既存機能の役割統合を優先。
  - 1画面1責務ではなく、1ループ1役割で定義。
  - 学習成果が必ず `報酬` か `次の推奨行動` に返るようにする。

## Future Conversation Plan
- 現時点では、キャラ画面の交流体験を `OpenAI API 前提の自由会話` に依存させない。常設の主機能はローカル会話で成立させる。
- 将来の会話機能は `通常会話` と `AI会話` を分離して設計する。
  - `通常会話`: ローカルデータで返す短文会話、好感度反応、学習後コメント、時間帯セリフ。
  - `AI会話`: 解放式または回数制限付きの特別機能。常時必須にはしない。
- UI 上の入口は将来的に `話す` よりも `交流` を基本概念にする。内部で `ひとこと / イベント / AI会話` へ分岐できるようにする。
- AI会話導入前に、以下のローカル会話基盤を先に整備する。
  - キャラ人格設定
  - 好感度段階ごとの口調差分
  - 学習状況や直近行動に応じた返答テンプレート
  - 朝 / 放課後 / 夜など時間帯別の定型セリフ
- AI会話を入れる場合も、返答生成の前段に `会話意図分類` を置く。
  - 例: 雑談 / 勉強相談 / 励まし / ツン反応 / デレ反応
  - まず意図を決めてから応答を組み立てることで、キャラぶれとコストを抑える。
- AI会話はフロントエンドから直接 API を叩かず、サーバー経由で実行する。
- AI会話導入時に必要な要件:
  - 回数制限または消費制
  - 会話履歴の短期保持と要約
  - キャラ設定の固定プロンプト
  - 失敗時の定型文フォールバック
  - コスト監視
  - 安全対策と不適切応答の制御
- 技術スタック面では、将来 AI 会話を追加する場合も、まずは既存の `Firebase` または軽量なサーバー経由で成立する最小構成を優先する。新しい大きな基盤追加は、利用実績と要件が固まってから判断する。

## Future Reflection Plan
- `日記機能` は、キャラとの雑談の代わりではなく `学習内容を自分の言葉で残す定着導線` として扱う。
- 基本ループは `学習/復習/対戦のあとに短く振り返る -> 記録が残る -> 次の学習や復習優先度に返る` を目指す。
- 初期段階では、自由記述を中心にしつつも `今日できたこと / つまずいたこと / 次にやること` の3項目テンプレートを用意する。
- 書いた内容は、まず `ローカル保存` を前提にして、学習履歴と一緒に日付単位で見返せるようにする。
- 日記は評価機能ではなく、`言語化`, `自己効力感`, `継続習慣` を支える補助機能として扱う。
- 将来的には以下の拡張余地を持たせる。
  - キャラからの短いねぎらいコメント
  - 学習量や正答率を自動で差し込むサマリー
  - 復習優先項目との連携
  - 週ごとのふり返り表示
- AIを使う場合も、最初から全文生成には寄せず、まずは `ふり返りの問いかけ候補` や `要約補助` のような補助用途から始める。

## Implementation Order
1. アプリ全体ループの文章化。
   - 各画面を `準備 / 学習 / 定着 / 実戦 / 報酬 / 管理` に分類する。
2. 指標の統一。
   - `stats` と保存データのうち、主ループに必要な値と不要な散在値を整理する。
3. ホーム導線の再設計。
   - 今日やるべき行動を1つか2つに絞って提示する。
4. 学習 -> 復習 -> 対戦 の接続強化。
   - 直前の学習内容や弱点が次の画面に自然に渡るようにする。
5. 報酬と育成の整理。
   - ダイヤ、知力、好感度、ランク上昇の意味を重複なく再定義する。
6. 最後に演出調整。
   - キャラ反応、ミッション文言、ランキング表示、進捗演出を主ループに合わせて整える。
7. 学習の言語化を支える日記・ふり返り導線を設計する。
   - `今日できたこと / つまずいたこと / 次にやること` を最小入力単位にして、学習履歴とつながる形を定義する。
8. 将来の交流拡張に備えて、ローカル会話レイヤーを整備する。
   - `ひとこと / 好感度会話 / 学習後コメント / 時間帯会話` をデータ駆動で返せるようにする。
9. AI会話は最後段階の拡張として扱う。
   - 常設コア機能化はせず、制限付きの特別機能として検証導入する。

## Test Plan
- 新規ユーザーが `タイトル -> キャラ選択 -> ホーム -> 学習 -> 復習 -> ホーム復帰` を迷わず完走できる。
- 学習後に、次に行くべき画面がホーム上で明確に提示される。
- 復習対象があるとき、対戦より復習を優先すべき理由がUIで伝わる。
- 対戦後の結果が `報酬 / 弱点 / 次の推奨行動` に反映される。
- 試験日設定がある場合、日数と進捗の関係がホームと目標画面で一貫して見える。
- 既存セーブデータでも破綻せず、欠損フィールドはデフォルト補完される。

## Working Rules
- `PLAN.md` をアプリ全体の仕様と進行方針の正本として扱う。機能追加や方向転換が起きたら、先にここを更新する。
- フロントエンドは既存の `React + Vite` を前提に進める。`Next.js` 前提の提案は、この repo では採用しない。
- レンダリングやゲーム進行は、まず既存の `React / CSS / CharacterStage / current battle & review flows` で成立させる。新規に `Phaser` や `PixiJS` を入れるのは、既存構成では限界があると確認できた場合だけにする。
- 永続化は既存の `localStorage + saveUtils + Firebase` を優先する。`Postgres` や `Redis` は、明確な要件が出るまでは追加しない。
- 各機能の実装後は、少なくとも `npm test` と `npm run build` を実行してから完了扱いにする。
- UI やゲームプレイに変更が入った場合は、ローカルで起動してブラウザ確認まで行う。
- 実装メモや検証記録は `.logs/` 配下に置く。長文の調査メモ、確認結果、作業手順の保存先として使う。
- 画像生成でアセットを作った場合は、使ったプロンプトを `.prompts/` 配下に保存する。
- 実装は大きな全面改修より、小さく焦点の定まった差分を優先する。

## Assumptions
- アプリの主軸は `対戦中心` だが、最終目標は `試験攻略` とする。
- 独立した新作ミニゲームは前提にせず、既存機能の再編成で全体をゲームとして成立させる。
- 実装フェーズでは、既存の `Home`, `StudySelect`, `Review`, `MultiplayerMatch` を優先的に整理対象とする。
